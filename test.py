import time
print(time.time())

#==========================================
#ТЕСТ ВСЕХ ВОЗМОЖНОСТЕЙ ЯЗЫКА RUPY
#==========================================

#1. ТЕСТ КЛАССОВ И МЕТОДОВ (ООП)
class Робот:
    def __init__(self, имя, модель):
        self.имя = имя
        self.модель = модель
        self.энергия = 100
        self.инвентарь = [] # Тест списков внутри класса

    def статус(self):
        print("--- ОТЧЕТ СИСТЕМЫ ---")
        print("Имя: " + self.name)
        print("Заряд: " + str(self.энергия) + "%")
        print("Предметы: " + str(self.инвентарь))

    def взять_вещь(self, предмет):
        self.инвентарь.append(предмет)
        print(self.name + " положил в сумку: " + предмет)

    def работа(self, задача):
        if self.энергия > 20:
            print("Выполняю: " + задача)
            self.энергия = self.энергия - 30
        else:
            print("НИЗКИЙ ЗАРЯД! Работа невозможна.")

#2. ТЕСТ ОБЫЧНЫХ ФУНКЦИЙ
def расчет_мощности(а, б):
    return а * б

#3. ОСНОВНАЯ ЛОГИКА
print("=== ЗАПУСК ПРОГРАММЫ RUPY ===")

#Тест ввода
оператор = input("Представьтесь, создатель: ")
print("Приветствую, " + оператор + "!")

#Тест создания объекта
бот = Робот("Валли", "Уборщик")

#Тест работы со списками and методами
бот.взять_вещь("Старая ложка")
бот.взять_вещь("Растение")
бот.взять_вещь("Болт")

#Тест удаления из списка
print("Нужно выкинуть мусор...")
бот.инвентарь.remove("Болт")

#Тест циклов (3 раза)
print("Проверка моторов...")
for _ in range(3):
    print("Пип-буп!")

#Тест циклов со счетчиком
итераций = 2
for номер in range(итераций):
    print("Шаг теста № " + str(номер + 1))


#==========================================
#НОВЫЙ БЛОК: ТЕСТ ЛОГИЧЕСКИХ ОПЕРАТОРОВ
#==========================================
print("--- ТЕСТ ЛОГИКИ ---")
доступ_разрешен = Истина
система_сломана = Ложь

if доступ_разрешен and not система_сломана:
    print("Логика И and НЕ работает: Доступ в систему открыт!")

заряд_аккумулятора = 15
критический_режим = Истина

if заряд_аккумулятора < 20 or критический_режим:
    print("Логика ИЛИ работает: Робот переходит в энергосбережение!")


#==========================================
#НОВЫЙ БЛОК: ТЕСТ СИСТЕМЫ С ФАЙЛАМИ
#==========================================
print("--- ТЕСТ СИСТЕМЫ ФАЙЛОВ ---")

#Тест записи в файл
with open "лог_робота.txt" "w" as файл_логирования:
    файл_логирования.write("Запись №1: Робот успешно инициализирован.\n")
    файл_логирования.write("Запись №2: Обнаружен русский текст в файлах!\n")

print("Файл успешно записан!")

#Тест чтения из файла
with open "лог_робота.txt" "r" as читаемый_файл:
    лог_данные = читаемый_файл.read()
    print("Содержимое созданного файла:")
    print(лог_данные)


#==========================================
#ОКОНЧАНИЕ ТЕСТА
#==========================================

#Тест условий and математики
рез = расчет_мощности 10 5
if рез > 40:
    print("Мощность в норме: " + str(рез))

#Тест обработки ошибок (Try/Except)
try:
    print("Проверка деления на ноль...")
    ошибка = 10 / 0
except Exception as е:
    print("ОШИБКА: Система перехватила деление на ноль!")
    print(e)

#Финальный статус
бот.работа("Уборка мусора")
бот.статус()

print("=== ТЕСТ ЗАВЕРШЕН УСПЕШНО ===")